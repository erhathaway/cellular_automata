import { css } from 'emotion';

const className = css`
  background-color: yellow;
  height: 100px;
  width: 200px;
`;

const app = {
  $cell: true,
  id: "menu",
  class: className,
}

export default app;
